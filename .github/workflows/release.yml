name: Release
on:
  push:
    branches: [main]

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Auto Release
        uses: Shiro-nn/release-helper@v1
        with:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
